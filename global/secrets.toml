# Names of secrets every machine carries. Values live only in ~/.rig/secrets.json
# on each machine (`bin/rig secret set NAME`); apply injects them into the harness env.
[secrets]
FILE_HOST_TOKEN = "upload token for https://file.bobbyradford.com (file-host repo)"
